class Solution {
    public int findDuplicate(int[] arr) {
        // code here
        int n=arr.length;
        long sum=0;
        for(int num:arr){
            sum+=num;
        }
        long expectedSum=(long)(n-1)*(n)/2;
    return (int)(sum-expectedSum);
    }
}
